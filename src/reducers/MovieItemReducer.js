const initialState = {
    movie: {}
};

const movieItemReducer  = (state = initialState, action = {}) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default movieItemReducer;